# Linguist Pro - Language Learning App

Многоязычное приложение для изучения английского языка с поддержкой русского и армянского языков.

## 🌟 Особенности

- 📚 **Два режима обучения**: Детский (100 слов) и Взрослый (1500+ слов)
- 🎯 **Три уровня сложности**: Easy, Medium, Advanced
- 📖 **Интерактивный словарь** с транскрипцией и изображениями
- 🔊 **Text-to-Speech** через браузерный API
- 📊 **Отслеживание прогресса** в Firebase
- 🔐 **Безопасная аутентификация** через Firebase Auth
- 💾 **Синхронизация данных** в облаке

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Разработка

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## 🔧 Технологии

- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Firebase** - Аутентификация и база данных
- **Tailwind CSS** - Стилизация (через CDN)

## 📦 Структура проекта

```
LinguaApp/
├── App.tsx              # Главный компонент приложения
├── authService.ts        # Сервис аутентификации
├── speechService.ts      # Text-to-Speech сервис
├── firebase.ts          # Конфигурация Firebase
├── wordData.ts          # Загрузка словарных данных
├── types.ts             # TypeScript типы
├── constants.ts         # UI строки и константы
└── imageLibrary.ts      # Работа с изображениями
```

## 🔐 Настройка Firebase

1. Создайте проект в [Firebase Console](https://console.firebase.google.com/)
2. Включите **Authentication** → **Email/Password**
3. Создайте **Firestore Database**
4. Настройте правила безопасности:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

5. Скопируйте конфигурацию Firebase в `firebase.ts`

## 📝 Использование

1. **Регистрация**: Создайте аккаунт с логином (минимум 6 символов) и паролем
2. **Выбор режима**: Переключитесь между детским и взрослым режимом в настройках
3. **Изучение**: Используйте раздел "Учеба" для изучения новых слов
4. **Повторение**: Раздел "Повторение" для закрепления материала
5. **Тест**: Доступен после изучения 20+ слов
6. **Словарь**: Просматривайте все изученные слова

## 🌐 Деплой на GitHub Pages

```bash
npm install -g gh-pages
npm run deploy
```

Приложение будет доступно по адресу: `https://[username].github.io/LinguaApp/`

## 📄 Лицензия

MIT

## 👤 Автор

lethifold222
